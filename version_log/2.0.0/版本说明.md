# 版本 2.0.0 — 版本说明

- 版本号：2.0.0
- 日期：2026-08-07
- 状态：**问题 2 实现完成**（概率云理论层 + Monte Carlo 验算层，含单元测试、结果、图表）
- 上版本：1.0.0（问题 1 确定性导通判定）

## 本版本新增内容

### 新增代码模块（`version_log/2.0.0/conductive_microstructure/`）

| 模块 | 说明 |
|---|---|
| `src/generation/` | 随机生成层：球面均匀方向（z=cosθ 采样）、盒内均匀位置、5000nm 圆柱解析切段回绕为"在箱片段"（与附件格式一致） |
| `src/cloud/` | 概率云层：`q_AA(r)` 二体连接概率估计、`V_AA = 4π∫r²q dr`、平均连接度 `k̄ = ρ_A·V_AA` |
| `src/simulation/` | 验算层：`single_trial` 单次微构体仿真（AABB 宽相位 + 精确判距 + DSU）、`confidence`（Wilson / Clopper-Pearson / 95% 单侧下界） |
| `src/graph/spatial_index.py` | AABB 向量化候选对筛选（宽相位，O(N²) 判距不可行的解法） |
| `scripts/build_aa_cloud.py` | 概率云 CLI：`results/cloud/cloud_AA.csv` |
| `scripts/solve_problem2.py` | MC 主脚本：4φ × 双模式 × 2000 trials，串/并行可复现 |
| `src/visualization/` | 绘图：P_conn(φ)（双模式+CI）、k̄(φ)，中文字体 |

### 修改

| 文件 | 修改内容 |
|---|---|
| `src/graph/connectivity.py` | PERIODIC 端点合并从 O(N²) 双重循环改为**坐标哈希分组 O(N)**（行为不变，Q1 回归验证通过：组1/组2/组3 节点数仍为 9/39/357） |
| `requirements.txt` | 追加 `matplotlib`、`scipy` |

### 测试

- 单元测试从 16 个增至 **35 个**，全部通过（新增：生成层 7、概率云 6、仿真层 6）
- 新增 Q1 回归测试（PERIODIC 哈希化后三组结果不变）

## 问题 2 结果摘要（trials=2000, seed=42）

| φ (%) | N_A | 周期连续 P̂ | 仅几何回绕 P̂ | k̄ |
|---|---|---|---|---|
| 0.50 | 354 | 0.9150 | 0.9180 | 0.902 |
| 0.60 | 424 | 0.9505 | 0.9575 | 1.081 |
| 0.70 | 495 | 0.9750 | 0.9765 | 1.262 |
| 1.00 | 707 | 0.9965 | 0.9970 | 1.802 |

- V_AA = 2.5493×10⁹ nm³；k̄≈1 的区间正是 P_conn 快速上升区（概率云理论验证成立）
- 两种边界模式结果差 <0.7%，结论稳健
- 收敛性（M=500/1000/2000 CI 收窄）与种子波动（3 组 seed 均在 CI 内）均已验证
- 图表：`results/figures/problem2_p_conn.png`、`problem2_kbar.png`

## 文件清单

| 文件 | 说明 |
|---|---|
| `version_log/2.0.0/conductive_microstructure/` | 问题 2 完整工程（基线复制自 1.0.0 + 全部新增模块） |
| `version_log/2.0.0/问题2_说明文档.md` | 建模、参数、结果、概率云对照、结论 |
| `version_log/2.0.0/版本说明.md` | 本文件 |
| `修改日志.md`（工作区根） | 本次开发修改记录 |

## 复现命令

```powershell
cd version_log\2.0.0\conductive_microstructure
python scripts\build_aa_cloud.py
python scripts\solve_problem2.py --workers 8
python -m pytest tests -q
```

## 问题 3 状态（本阶段：脚本与测试就绪，主流程待运行）

- 脚本：`scripts/solve_problem3.py` 已就绪——在 φ∈[0.20%, 0.70%] 区间二分搜索满足 `P_conn ≥ 0.90` 的最低介质 A 体积分数 φ_min
  - 判定标准：**95% Wilson 单侧置信下界 `p_lower,95% ≥ 0.90`**（非简单点估计 p̂≥0.90，提供统计保证）
  - 精度：φ 绝对精度 `tol=0.0001`（0.01%），最大迭代 40，二分中点 `trials=1000`
  - 收敛后 φ_min 上下邻点用 `confirm_trials=4000` 复核；双边界模式对比（periodic_connected / wrapped_geometry_only）
  - 可复现：seed=42，`--workers 8` 并行；含概率云理论对照 φ_theory=V_A/V_AA≈0.5546%（k̄=1 临界）
- 单元测试：`tests/test_problem3.py`（mock 单元测试）通过
- **主流程 Monte Carlo 尚未运行**，φ_min 结果待运行后补录（运行命令见 `问题3_说明文档.md`）

## 下一步计划

1. 问题 3：完成主流程运行并补录 φ_min 结果（脚本已就绪，运行命令见 `问题3_说明文档.md`）
2. 问题 4：A/B 混合体系（B 球成本 0.05 元/μm³、A 成本 1.05 元/μm³）的最低成本组合
3. 后续版本变更时新建 `version_log/<新版本号>/` 归档
