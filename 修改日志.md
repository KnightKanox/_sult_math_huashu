# 修改日志

> 约定：按时间倒序记录每次代码/文档修改；每次记录包含日期、版本、修改内容、涉及文件。

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 4 主流程运行完成：最低成本组合结果与文档补录

### Q4 运行结果（seed=42, trials=500, confirm_trials=4000，总耗时 135.4 分钟）

| boundary_mode | N_A* | N_B* | 最低成本（元） | p̂ | p_lower,95% |
|---|---|---|---|---|---|
| periodic_connected | 360 | 0 | 5.3438 | 0.9147 | 0.9072 |
| wrapped_geometry_only | 340 | 50 | 5.1307 | 0.9095 | 0.9018 |

- **结论：最低成本组合以介质 A 为主，B 球仅作少量补充**。B 球单价便宜但体积大（0.0335 μm³ vs A 的 0.0141 μm³）、点接触导电效率低，纯 B 体系需数千颗（成本 12+ 元）远不如以 A 为主；在 N_A 临界区加 50 颗 B（0.08 元）可替代 10 根 A（省 ≈0.15 元），wrapped 最优 (340,50) 优于纯 A 的 (350,0)
- 概率云双介质扩展：V_BB=2.7172×10⁸ nm³（解析）、V_AB=9.0877×10⁸ nm³（dr=25 数值积分，与胶囊近似偏差 1.4%）；λ_max 理论可行区 N_A∈[0,360], N_B∈[400,5000] 与实际 MC 边界方向一致但更宽松（实际 N_A<310 时 N_B=5000 仍不够）——均值场判据 vs 链式渗透，与 Q3 观察同源
- 统计临界如实记录：periodic (350,0) 与 wrapped (340,0) 4000 次复核 p_lower=0.8997/0.8974 略低于 0.90（二分 500 次判定曾达标，抽样波动）；边界单调性校验报 3 处违反（500 trials 波动），Level 3 以 4000 次确认修正
- 新增输出：`results/problem4/problem4_result.csv` / `.json`（含 q_AB 曲线、理论网格、二分 history、confirm 记录、最优解）/ `problem4_lambda_max_heatmap.png` / `problem4_mc_pconn_heatmap.png` / `problem4_cost_boundary.png`
- 文档：新建 `问题4_说明文档.md`；更新 `版本说明.md` 追加"问题 4 状态"段落

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 修复：Level 2 二分评估漏传共享进程池（每评估重建 8 worker）

### 问题

上一轮性能优化把共享 `ProcessPoolExecutor` 传入了 Level 3 确认与 Level 2.5 粗网格，但 **Level 2 的 `make_nb_evaluator` 调用漏传 `executor`**，导致 `run_trials_mixed` 每次二分评估都自建并销毁 8 个 worker 进程（Windows spawn 每次 ~2-3s 启动开销）。实测主流程运行 40 分钟仅推进数个 N_A 点，且 `Get-Process` 观测到 worker 每 30-40s 整批重建（旧 PID 消失 → 新 PID 出现），与"每评估重建池"节奏吻合——这也解释了此前 CPU 统计出现负增量的现象（按旧 PID 快照差值为 0/负）。

### 修改（`version_log/2.0.0/conductive_microstructure/`）

| 文件 | 修改内容 |
|---|---|
| `scripts/solve_problem4.py` | Level 2 构造 `make_nb_evaluator` 时补传共享 `executor`（与 Level 3/2.5 一致），消除每次二分评估的进程池重建开销 |

### 验证

- 小规模冒烟（`--n-a-max 30 --n-b-max 500 --trials 30 --workers 4`）：Level 2 双模式边界单调性 OK、Level 2.5 完成，总耗时 4.2s，无异常
- 结果不受影响：子种子池机制保证串/并行（worker 数无关）结果一致，本次仅性能修复
- 附带发现：控制台默认 GBK 编码无法输出 `³`（nm³），运行需设 `PYTHONIOENCODING=utf-8`（仅影响 stdout 显示，不影响结果文件）

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 4 性能优化：B-B 宽相位换 cKDTree、A-A 加包含直线预筛、共享进程池

### 背景

首次试跑 Q4 完整三级流程（trials=500, confirm=4000, N_A∈[0,360]×37 网格、N_B 二分 [0,5000]）预估 **~15 小时不可行**。经 cProfile 定位三个瓶颈并修复，单次混合仿真耗时下降 3~4 倍，总运行预估收敛到 1 小时量级（与 Q2 的 28.5 分钟同量级）。

### 修改（`version_log/2.0.0/conductive_microstructure/`）

| 文件 | 修改内容 |
|---|---|
| `src/graph/spatial_index.py` | `ball_candidates` 由 O(N²) 距离矩阵（N=5000 时 25M 元素 ~200MB）改为 `scipy.spatial.cKDTree.query_pairs` 精确枚举（O(N log N)）；新增 `aabb_candidates_fast(segs, threshold)`：AABB 宽相位后叠加"包含直线距离"向量化预筛（线段距离 ≥ 包含直线距离，故直线距离 > 阈值必不导通、无漏判），把 A-A 精确判距调用量从 ~1.6 万降到数百 |
| `src/simulation/single_trial.py` | B 球电极判定由 5000 次 Python 调用改为 numpy 掩码向量化（\|plane_x - c_x\| ≤ BE_AXIS_THRESHOLD）；B-B 连边去掉冗余的 `spheres_connected` 精确复核（cKDTree 已返回精确 ≤ 阈值对）；A-A 连边改用 `aabb_candidates_fast`；同步清理未再使用的导入 |
| `scripts/solve_problem4.py` | Level 2/2.5/3 复用 main 一次性创建的 `ProcessPoolExecutor`（`executor` 参数贯穿 `run_trials_mixed`/`make_nb_evaluator`），消除每次二分评估重建 8 个 worker 进程的启动开销 |

### 验证

- 单次混合仿真耗时（含 scipy 首次懒导入）：纯B(5000) 1685→402ms、纯A(360) 488→51ms、混合(200,2000) 656→121ms、混合(360,2500) 1333→275ms
- 纯A一致性：同种子 `run_single_trial`（原路径）与 `run_single_trial_mixed(n_b=0)`（快速宽相位）8 个种子逐字段（connected/node_count/segment_count/edge_count/电极数/最大分量占比/平均度）**0 差异**
- `python -m py_compile` 3 个文件通过；全量 `python -m pytest tests -q` **56 passed**

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 4 单元测试：新增 tests/test_problem4.py（新文件，未改动任何 src/ 源码）

### 新增文件（`version_log/2.0.0/conductive_microstructure/`）

| 文件 | 说明 |
|---|---|
| `tests/test_problem4.py` | 问题 4（A/B 混合介质最低成本组合）单元测试，14 个用例，全部快速（mock/小样本/monkeypatch 注入固定几何，不跑主流程与重型 MC） |

### 覆盖项

- B 球生成：`generate_b_spheres` 球心全在 [-4800,4800]³ 内、n_b=0 返回 (0,3) 空数组
- 判据临界（直接构造几何）：B-B 中心距 401.8 导通/401.81 不导通；A-B 点-线段距 231.8 导通/231.81 不导通；B-电极距平面 ≤201.8 导通（±0.01 裕度逼近，因 double 减法舍入 201.8 恰不可表示）并验证左/右平面与 x 分量方向性
- 混合单 trial：纯 B（n_a=0,n_b=50）字段齐全不崩溃；纯 A 与 `run_single_trial` 同种子逐字段一致；monkeypatch 注入"A 触左电极-B 球链-A 触右电极"固定几何 → 两种模式 connected=True；同 seed 两次调用全部字段一致
- 概率云：q_bb(401.8)=1、q_bb(401.81)=0；v_bb_analytic() 与 4/3π·401.8³ 相对误差 <1e-6；estimate_v_ab(r_max=2731.8, dr=25, M=800) 与 v_ab_capsule_approx() 相对偏差 ≤15%（实测约 0.98%）
- 连接矩阵/λ_max：对称、元素非负、λ_max≥0、λ_max 随 n_a 增大不降；total_cost 单位换算（nm³→μm³）与手算一致且随 N_A/N_B 单调不减；Wilson 沿用 Q3 判据（950/1000≥0.90、850/1000<0.90）

### 验证

- `python -m py_compile tests\test_problem4.py` 通过（退出码 0）
- `python -m pytest tests -q`：56 passed（既有 42 + 新增 14），总耗时约 4.7s
- 发现并规避两处实现边界行为：`sphere_electrode_connected` 返回 np.bool_（用 == 而非 is 断言）；B-电极阈值 201.8 因 double 减法舍入不可精确到达（用 ±0.01 裕度）

---


### 修改（`version_log/2.0.0/conductive_microstructure/`，纯追加、未改动现有函数行为）

| 文件 | 修改内容 |
|---|---|
| `src/graph/spatial_index.py` | 追加 `aabb_candidates_mixed(segs, sphere_centers, threshold)`（线段 AABB vs 球心点，返回 (K,2) 线段-球候选对，无漏判）与 `ball_candidates(centers, threshold)`（球心对 i<j 且中心距 ≤ 阈值的候选对）；均沿用既有"逐轴间隔距离平方累加、避免 (N,N,3) 大内存"的宽相位风格 |
| `src/simulation/single_trial.py` | 追加 `run_single_trial_mixed(rng, n_a, n_b, mode)`：随机生成 n_a 根 A 圆柱 + n_b 个 B 球；A 节点（`_build_nodes`）+ B 球节点（索引偏移 m_a）统一建 DSU；电极连接按 A 轴线段/B 球心分别登记左右电极；A-A/A-B/B-B 三种连边均先 AABB 宽相位筛候选再精确判距、统计跨节点成功连边数；返回字段与 `single_trial_from_segments` 完全一致；n_a=0 或 n_b=0 均正常（空数组安全）；import 相应追加 |

### 验证

- `python -m py_compile` 2 个文件全部通过（退出码 0）
- 冒烟：pureB（n_a=0,n_b=50）→ connected=False、node_count=50；mixed（30,40）→ node_count=94；纯A一致性（同种子 n_b=0 与 `run_single_trial`）→ True；全空（0,0）→ 全 0 统计

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 4 主脚本 solve_problem4.py：A/B 混合体系最低成本组合（新文件，未运行主流程）

### 新增文件（`version_log/2.0.0/conductive_microstructure/`，未改动任何其他文件）

| 文件 | 说明 |
|---|---|
| `scripts/solve_problem4.py` | 问题 4 主脚本，三级流程：Level 1 λ_max 理论筛选（N_A 步长 10 × N_B 步长 100 网格，λ_max≥1 为理论可行区）→ Level 2 逐 N_A 网格点整数二分最小 N_B（判据 `wilson_one_sided_lower ≥ 0.90`，边界单调性校验）→ Level 2.5 MC 粗网格热图数据 → Level 3 沿边界取成本最小候选 + 8 邻域 confirm_trials 复核得到最优 (N_A*, N_B*)；成本 `total_cost`（1.05·N_A·V_A/1e9 + 0.05·N_B·V_B/1e9 元） |

### 模块函数清单

- `parse_args`：CLI `--n-a-max 360 --n-a-step 10 --n-b-max 5000 --trials 500 --confirm-trials 4000 --seed 42 --mode all --workers 8 --out-dir --plot/--no-plot --theory-only --ab-samples 500 --r-dr 25 --mc-grid-na-step 60 --mc-grid-nb-step 1000 --mc-grid-trials 300`
- `lambda_max_grid(n_a_pts, n_b_pts, v_aa, v_ab, v_bb)`：返回 (n_a_grid, n_b_grid, lam_grid)
- `run_trial_batch_mixed` / `run_trials_mixed`：混合单次仿真批处理 + ProcessPoolExecutor 并行（仿照 solve_problem2.run_trials；`run_single_trial_mixed` 由 T3 任务提供，函数内延迟导入保证模块可先 import）
- `make_nb_evaluator`：固定 (mode, N_A) 的二分评估回调（按调用顺序取子种子池行，可复现）
- `binary_search_min_nb(n_b_low, n_b_high, evaluate)`：整数二分，边界情形 evaluate(0) 满足→0、evaluate(high) 不满足→None；返回 {n_b_min, iterations, history}
- `make_confirm_points`：候选 + 8 邻域（da∈{-step,0,step}、db∈{-50,0,50}，≤9 点）
- `write_csv`：`problem4_result.csv`（boundary_mode,n_a,n_b_min,cost,p_hat,p_lower,iterations）
- `setup_matplotlib` / `plot_lambda_max_heatmap`（Figure 6）/ `plot_mc_pconn_heatmap`（Figure 7）/ `plot_cost_boundary`（Figure 8）：Agg 后端 + Microsoft YaHei 字体 + mathtext 组合字符
- `main`：数据准备（V_AA=load_v_aa、V_BB=v_bb_analytic、V_AB=estimate_v_ab 种子 seed+1000、q_AB(r) 曲线种子 seed+2000）→ 三级流程 → JSON/CSV → 绘图（失败不阻断）

### 可复现机制

主 rng 一次性派生三个子种子池：`bisect_pool`(n_modes, n_na_pts, 20, trials)、`confirm_pool`(n_modes, 9, confirm_trials)、`mc_grid_pool`(n_modes, n_mc_pts, mc_grid_trials)；评估顺序固定（逐模式、逐 N_A、逐二分迭代），串/并行结果一致；`--theory-only` 只做 V_AB/V_BB + Level 1 + 输出与 Figure 6。

### 验证

- `python -m py_compile scripts\solve_problem4.py` 通过（退出码 0）
- import 冒烟通过：`import solve_problem4` 无 ImportError（模块顶层只做定义与常量）
- 纯逻辑自检通过：整数二分收敛（阈值 2500→n_b_min=2500、0 满足→0、上界不满足→None）、lambda_max_grid 形状 (37,51)、确认点 9/6 个、write_csv None 处理、三张图合成数据渲染成功
- 主流程未运行（V_AB 采样耗时，由用户另行运行；依赖的 `run_single_trial_mixed` 待 T3 落地）

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 4 A/B 混合介质扩展：4 个文件追加 B 介质常量与判据函数

### 修改（`version_log/2.0.0/conductive_microstructure/`，纯追加、未改动任何现有常量/函数行为）

| 文件 | 修改内容 |
|---|---|
| `src/config.py` | 追加 `AB_SEG_THRESHOLD=231.8`（A轴线段-球心导通阈值）、`BB_CENTER_THRESHOLD=401.8`（球心距阈值）、`BE_AXIS_THRESHOLD=201.8`（球-电极平面阈值）、`B_SPHERE_BOX_HALF=4800.0`（B球生成范围半边长）、`COST_A_PER_UM3=1.05`、`COST_B_PER_UM3=0.05`（成本）、`NM3_PER_UM3=1e9`（体积换算） |
| `src/generation/medium_generator.py` | 追加 `generate_b_spheres(rng, n_b)`：球心在 [-4800,4800]³ 均匀采样返回 (n_b,3) 数组（n_b=0 支持空数组）；import 追加 `B_SPHERE_BOX_HALF` |
| `src/geometry/segment_distance.py` | 暴露公开接口 `point_segment_distance(p, a, b)`：委托内部 `_point_segment_dist` 计算点到线段最短距离 |
| `src/geometry/cylinder_distance.py` | 追加 `cylinder_sphere_connected`（A-B 导通）、`spheres_connected`（B-B 导通）、`sphere_electrode_connected`（B-电极导通）；import 追加 `A_RADIUS`、`B_RADIUS`、`point_segment_distance` |

### 验证

- `python -m py_compile` 4 个文件全部通过（退出码 0）

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 3 新增双介质概率云模块（A 圆柱 + B 球，新文件，未运行主流程）

- 新增文件 `version_log/2.0.0/conductive_microstructure/src/cloud/mixed_cloud.py`（概率云层混合介质模块，未改动其他代码文件）：
  - `ab_max_contact_center_distance()`：A-B 最大连接中心距 = A_LENGTH/2 + R_AB = 2731.8 nm（r 超过 q_AB=0）
  - `estimate_q_ab(r, rng, samples_per_r=2000)`：A-B 导通概率 MC 采样（A 轴方向与 B 球心方向球面均匀，判据 point_segment_distance ≤ R_AB=231.8），返回导通比例
  - `q_bb(r)`：B-B 解析导通概率（r ≤ 2·B_RADIUS+δ = 401.8 为 1，否则 0）
  - `estimate_v_ab(r_max=None, dr=25.0, rng=None, samples_per_r=2000)`：V_AB = 4π∫r²·q_AB(r)dr 数值积分（np.trapezoid，r 网格约定同 build_aa_cloud）
  - `v_bb_analytic()`：V_BB = 4/3·π·401.8³
  - `v_ab_capsule_approx()`：胶囊近似 V = π·R_AB²·A_LENGTH + 4/3·π·R_AB³
  - `connection_matrix(n_a, n_b, v_aa, v_ab, v_bb)`：对称 2×2 连接矩阵（ρ_A·V_AA / ρ_B·V_AB / ρ_B·V_BB）
  - `lambda_max(m)`：对称矩阵最大特征值（np.linalg.eigvalsh）
  - `total_cost(n_a, n_b)`：成本函数（COST_A_PER_UM3·N_A·V_A/1e9 + COST_B_PER_UM3·N_B·V_B/1e9）
- 依赖说明：`point_segment_distance`（src.geometry.segment_distance）与 `COST_A_PER_UM3`/`COST_B_PER_UM3`（src.config）为 T2 任务约定新增，先假定存在
- 验证：`python -m py_compile src\cloud\mixed_cloud.py` 通过；未运行主流程

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ Q3 跨设备独立复现验证：结果逐字节一致

- 另一台设备（Windows，`C:\Users\zdl\1111\_sult_math_huashu`，从公开仓库 clone 后独立运行 `solve_problem3.py`）重跑 Q3 主流程，结果目录位于 `Q3re_test/version_log/2.0.0/conductive_microstructure/results/problem3/`
- 对比结论：**与本地结果完全一致，无冲突**：
  - `problem3_result.json` 经 `git diff --no-index` 比对退出码 0，**零差异**（含全部二分 history、confirm 记录）
  - `problem3_result.csv` / `problem3_confirm.csv` 逐字节一致，**连 4000 次复核的成功次数都相同**（3671/3662/3623/3630）
  - φ_min（0.5125% / 0.4969%）、k̄（0.9254 / 0.8948）、6 轮迭代收敛、V_AA=2.5493×10⁹ nm³、φ_theory=0.5546% 全部吻合
- 意义：验证了"主 rng 一次性派生子种子池 + 固定评估顺序"的可复现机制在**跨设备/跨平台**下依然成立，MC 随机序列逐位一致——这是 Q3 结论与概率云理论验证的独立复现背书
- 附注：该设备 `run_err.log` 有 `Glyph 772 (COMBINING MACRON) missing` 绘图字体警告（仅影响图中 k̄ 渲染美观、不影响数值），源于其 matplotlib 版本对 mathtext `\bar` 组合重音字形的处理差异，本地已无此警告

---

## 2026-08-08 ｜ 版本 2.0.0 ｜ 问题 3 主流程运行完成：φ_min 结果与文档补录

### Q3 运行结果（seed=42, trials=1000, confirm_trials=4000）

| boundary_mode | φ_min | N_A | p̂ | p_lower,95% | k̄ | φ_theory |
|---|---|---|---|---|---|---|
| periodic_connected | 0.5125% | 363 | 0.9155 | 0.9080 | 0.9254 | 0.5546% |
| wrapped_geometry_only | 0.4969% | 351 | 0.9075 | 0.8997 | 0.8948 | 0.5546% |

- 两模式均 6 轮二分迭代收敛（理论预估 log₂5.6≈6 轮）；φ_min 落在预期区间 0.45%~0.55%，且**略低于**理论临界 φ_theory=0.5546%（细长圆柱链式连接使 P_conn≥0.90 可在 k̄<1 时达到），与规划文档 §71 偏差来源分析一致——概率云理论定量互证达成
- 双模式差 0.016%，比 Q2 双模式差（<0.7%）更小，结论稳健
- 统计临界如实记录：wrapped φ_min 处 4000 次复核下界 **0.8997 略低于 0.90**（二分 1000 次判定 0.9058≥0.90 达标，属统计临界）；periodic φ_min 处 0.9080≥0.90 达标
- 新增输出：`results/problem3/problem3_result.csv` / `problem3_confirm.csv` / `problem3_result.json`（含二分 history）/ `problem3_bisection.png`
- 修复二分评估子种子未按 trials 切分的 Bug（此前整行 4000 个种子全传导致 p>1、Wilson 开方报错），新增防回归测试 `test_mc_evaluator_uses_only_trials_seeds`（monkeypatch `run_trials` 断言只消耗 trials 个种子），全量 42 passed
- 文档补录：`问题3_说明文档.md` §6 占位表替换为真实结果并加统计临界说明、§7 追加实测验证；`版本说明.md` 更新 Q3 状态与结果

---

## 2026-08-07 ｜ 版本控制初始化 ｜ 推送至 GitHub 公开仓库

- 新建**公开**仓库 `KnightKanox/_sult_math_huashu`（GitHub REST API，`private=false`；token scopes: gist/repo/workflow）
- 新增根目录 `.gitignore`（排除 `.venv/`、`__pycache__/`、`*.py[cod]`、`.pytest_cache/`、`.idea/.vscode` 等，`git status` 确认 0 个 venv 文件入暂存）
- `git init -b main`；首次提交 100 个文件（Q1/Q2/Q3 代码、results、文档、specs、竞赛附件），commit message 经字节级校验为正确 UTF-8
- `git push -u origin main` 成功（122 objects / 4.28 MiB）；因 github.com 直连被重置，经本机代理 `http://127.0.0.1:7897`（会话环境变量方式，未改 git config）完成推送
- 仓库地址：https://github.com/KnightKanox/_sult_math_huashu

---

## 2026-08-07 ｜ 版本 2.0.0 ｜ 问题 3 编码任务：P_conn≥90% 最低填充率二分搜索（新文件，未运行主流程）

### 新增文件（`version_log/2.0.0/conductive_microstructure/`）

| 文件 | 说明 |
|---|---|
| `scripts/solve_problem3.py` | 问题 3 主脚本：`binary_search_min_phi`（可注入评估回调的二分核心，判定 p_lower(95%)≥0.90）、`make_mc_evaluator`（真实 MC+Wilson 下界包装）、`confirm_point`、`plot_bisection`、CSV/JSON 输出；CLI：--phi-low/--phi-high/--tol/--trials/--confirm-trials/--seed/--mode/--workers/--out-dir/--plot/--no-plot；可复现性采用主 rng 一次性派生 (模式×2·MAX_ITER+2×max(trials,confirm_trials)) 子种子池；**主流程未运行** |
| `tests/test_problem3.py` | 6 个 mock 快速测试（不跑真实 MC）：二分收敛、更小 tol 区间更紧、max_iter 上限抛 RuntimeError、Wilson 判定边界（950/1000≥0.90、850/1000<0.90）、φ↔N_A 与理论换算（N_A(0.005)=354、CYLINDER_VOLUME/2.5493e9≈0.005546）、history 完整性 |

### 验证

- `python -m py_compile scripts\solve_problem3.py` 通过
- `python -m pytest tests\test_problem3.py -q`：6 passed；全量 `python -m pytest -q`：**41 passed**（35 既有 + 6 新增）
- 理论常数核对：V_AA=2.5493×10⁹ nm³，φ_theory=CYLINDER_VOLUME/V_AA≈0.005546

---

## 2026-08-07 ｜ 版本 2.0.0 ｜ 问题 2 实现完成（概率云理论层 + Monte Carlo 验算层）

### 新增文件（`version_log/2.0.0/conductive_microstructure/`）

| 文件 | 说明 |
|---|---|
| `src/generation/__init__.py` | 生成层包入口 |
| `src/generation/random_orientation.py` | `random_unit_vector(rng)`：球面均匀单位向量（z~U(-1,1) + phi~U(0,2π)，避免 θ 采样极区聚集） |
| `src/generation/random_position.py` | `random_position_in_box(rng, box_half=5000.0)`：盒内均匀三维位置 |
| `src/generation/medium_generator.py` | 介质A圆柱生成核心：`_cross_times`（六盒面穿越时刻解析解）、`_wrap`（周期回绕）、`make_cylinder_segments`（显式构造切段）、`generate_a_cylinder`（随机单根）、`generate_batch`（n 根拼接为附件行格式 (M,6)） |
| `src/cloud/__init__.py` | 概率云层包入口 |
| `src/cloud/aa_cloud.py` | `estimate_q`（通用二体采样）、`make_aa_connected_func`、`estimate_q_aa(r, rng, samples_per_r)`、`aa_max_contact_center_distance()` |
| `src/cloud/effective_volume.py` | `effective_volume`（V_AA=4π∫r²q dr，np.trapezoid）、`mean_degree`（k̄=ρ·V_AA）、`num_density` |
| `src/simulation/__init__.py` | 仿真层包入口 |
| `src/simulation/confidence.py` | `wilson_ci`、`clopper_pearson_ci`（scipy beta.ppf）、`wilson_one_sided_lower`（95% 单侧下界，供 Q3） |
| `src/graph/spatial_index.py` | `aabb_candidates(segs, threshold)`：AABB 向量化宽相位（逐轴累加控内存，无漏判） |
| `src/simulation/single_trial.py` | `single_trial_from_segments(segments, mode)`、`run_single_trial(rng, n_a, mode)`：生成→切段→宽相位→精确判距→DSU→L/R 判定 |
| `src/visualization/__init__.py` | 绘图包入口 |
| `src/visualization/probability_plot.py` | `plot_p_conn`（P_conn(φ) 双模式+CI 误差棒）、`plot_kbar`（k̄(φ)），中文字体（Microsoft YaHei） |
| `scripts/build_aa_cloud.py` | 概率云 CLI → `results/cloud/cloud_AA.csv` |
| `scripts/solve_problem2.py` | MC 主脚本：4φ×双模式×2000 trials，`--workers` 并行，子种子机制串/并行结果一致 |
| `scripts/plot_theory_vs_simulation.py` | 仿真 vs 概率云理论对比图：左图双 y 轴对照（仿真 P_conn 含 95% CI 误差棒 vs 理论 k̄，标注 k̄=1 理论临界线），右图 P̂-k̄ 相图（标注各 φ 点与理论临界区） |
| `tests/test_generation.py` | 7 个测试（方向矩、位置、切段、周期合并 node_count==50、可复现） |
| `tests/test_cloud.py` | 6 个测试（q(0)=1、远距=0、BB 解析基准、V_BB 数值、Wilson sanity、可复现） |
| `tests/test_simulation.py` | 6 个测试（N_A=0 不导通、双圆柱链导通、AABB 无漏判、Q1 回归、可复现） |

### 修改

| 文件 | 修改内容 |
|---|---|
| `src/graph/connectivity.py` | PERIODIC 端点合并 O(N²) 双重循环 → **坐标哈希分组 O(N)**（round 到 6 位，行为不变）；回归测试断言附件三组节点数仍为 9/39/357 |
| `requirements.txt` | 追加 `matplotlib`、`scipy` |

### 关键验证与结果

- 单元测试 **35 passed**（原 16 + 新增 19）
- 概率云：`V_AA = 2.5493×10⁹ nm³`（r∈[0,5100]，步长 25，M=2000/点）；q(0)=1、r>5061.8 时 q=0；BB 解析基准校验框架正确（q_BB≈I(r≤401.8)、V_BB≈4/3π·401.8³）
- 性能：700 根圆柱（M≈1240 片段）单次仿真实测 ~0.55s（其中 AABB 宽相位 0.05s）；全量 16000 trials 用 `--workers 8` 约 28.5 分钟
- **主结果**（trials=2000, seed=42）：

| φ | N_A | 周期连续 P̂ | 仅几何回绕 P̂ | k̄ |
|---|---|---|---|---|
| 0.50% | 354 | 0.9150 | 0.9180 | 0.902 |
| 0.60% | 424 | 0.9505 | 0.9575 | 1.081 |
| 0.70% | 495 | 0.9750 | 0.9765 | 1.262 |
| 1.00% | 707 | 0.9965 | 0.9970 | 1.802 |

- 收敛性：M=500/1000/2000 的 P̂=0.932/0.913/0.915，CI 宽度 0.044→0.035→0.024 持续收窄
- 种子波动：seed=1/2/42 在 φ=0.50%、0.70% 下 P̂ 均在各自 CI 内
- 解读：k̄≈1 处 P_conn 快速上升（0.90→1.08 对应 0.92→0.95），概率云理论验证成立；双模式差 <0.7% 结论稳健

### 文档与归档

- 新建 `version_log/2.0.0/`，复制 1.0.0 工程为基线，在其上开发
- 新增 `version_log/2.0.0/问题2_说明文档.md`、`version_log/2.0.0/版本说明.md`
- 图表：`results/figures/problem2_p_conn.png`、`problem2_kbar.png`、`problem2_theory_vs_simulation.png`（仿真 vs 理论对比图：理论临界点 k̄=1 与仿真跃迁区对齐）

---

## 2026-08-07 ｜ 版本 1.0.0 ｜ 问题 1 实现完成 + 版本归档

### 新增文件（问题 1 工程 `conductive_microstructure/`）

| 文件 | 说明 |
|---|---|
| `src/config.py` | 全局常量与 `BoundaryMode` 枚举（PERIODIC_CONNECTED / WRAPPED_GEOMETRY_ONLY） |
| `src/geometry/segment_distance.py` | 线段-线段最短距离算法（Ericson 收缩参数化 + 平行/退化处理） |
| `src/geometry/cylinder_distance.py` | A-A 胶囊判据、A-电极判据 |
| `src/geometry/primitives.py` | `Node` 数据结构 |
| `src/graph/dsu.py` | 并查集（路径压缩） |
| `src/graph/connectivity.py` | 建图（周期合并）、DSU 连通判定、多源 BFS 贯通路径 |
| `src/io/attachment_reader.py` | openpyxl 读取附件三个分表 |
| `scripts/solve_problem1.py` | 主脚本：双模式对比，输出文本 + JSON |
| `tests/test_segment_distance.py` | 线段距离 6 个测试 |
| `tests/test_cylinder_distance.py` | 圆柱/电极 4 个测试 |
| `tests/test_dsu.py` | 并查集 3 个测试 |
| `tests/test_connectivity.py` | 连通性 3 个测试 |
| `requirements.txt` | numpy / openpyxl / pytest |
| `results/problem1/problem1_result.json` | 三组 × 双模式完整结果 |

### 关键 Bug 修复

1. **`test_touching_endpoint_segments_distance`（预期 0.0 实得 5.0）**：原线段距离算法在平行退化情形计算错误 → 重写 `segment_segment_distance`，增加 `_point_segment_dist` 辅助函数，平行/退化时枚举端点投影取最小。
2. **`test_skew_segments_distance`（预期 √2 实得 1.0）**：测试几何本身设计错误（垂距为 1），修正测试数据使最近点距离为 √2。
3. **`test_chain_broken`（应不导通却导通）**：测试数据第二条线段与第一条共面接触，且周期模式下边界端点合并导致误连 → 测试数据改为 y=100 错开。
4. **`test_periodic_merge_diff` IndexError**：WRAPPED 模式下片段数组形状为 (2,3) 而非 (1,2,3) → 统一 `np.array([[p[i], q[i]]])`。
5. **`test_chain_connected`（路径长度断言失败）**：周期模式下节点合并后路径仅 1 个节点 → 断言放宽为 ≥1，并额外断言行独立模式路径长度 ==3。
6. **WRAPPED 模式连通但贯通路径为空**：`_find_path` 只从最小左节点 BFS → 改为**多源 BFS**（所有左电极直连节点同时入队）。

### 数据结论（经代码核对确认）

- 附件"一行"= 边界截断后的在箱线段（同一根 5000nm 圆柱被切多段、分行存储）；
- 组1：12 行、组2：49 行、组3：535 行；组1/组2 为 ±500 薄板结构，组3 全盒。

### 问题 1 结果

| 分组 | PERIODIC_CONNECTED | WRAPPED_GEOMETRY_ONLY |
|---|---|---|
| 组1 | 导通（9 节点，合并 3 组） | 不导通（12 节点） |
| 组2 | 导通（39 节点，合并 10 组） | 导通（49 节点） |
| 组3 | 导通（357 节点，大量合并） | 导通（535 节点） |

16 个单元测试全部通过。

### 文档与归档

- 新增 `version_log/1.0.0/版本说明.md`（初始基线 → 更新为 Q1 完成状态）
- 新增 `version_log/1.0.0/问题1_说明文档.md`（建模与实现说明）
- 将 `conductive_microstructure/` 归档至 `version_log/1.0.0/conductive_microstructure/`
