# 现状描述

> 日期：2026-08-08  
> 用途：把当前项目已经形成的思路、结果和结论捋顺，便于后续继续建模、写论文和答辩说明。

## 1. 这道题我们现在的总思路

当前不是把题目拆成几个孤立小问分别硬算，而是已经形成了一条比较完整的统一主线：

`几何导通判据 -> 局部连接概率云 -> 等效连接体积 -> 随机几何图/连续渗流解释 -> Monte Carlo 验证 -> 二分/边界/成本优化`

对应理解如下：

1. **几何层**  
   先严格定义“两个导电介质什么时候算导通”。  
   对 A 圆柱，核心是线段-线段最短距离；对 B 球，核心是球心距、点到线段距离、球到电极平面距离。

2. **理论层（概率云）**  
   不直接研究整个盒子的复杂网络，而是先研究“固定一个导体后，另一个导体出现在某个相对位置时导通的概率”，得到 `q(r)`。  
   再把这个局部概率云积分成等效连接体积：
   - 纯 A：`V_AA`
   - 混合 A/B：`V_AB`、`V_BB`

3. **宏观解释层**  
   用平均连接度 `k̄ = ρ_A·V_AA` 或双介质连接矩阵的最大特征值 `λ_max` 来解释“为什么系统会从不导通走向导通”。

4. **数值验证层**  
   用完整三维 Monte Carlo 真正生成微构体，建图、判连通，算出 `P_conn`，再反过来验证理论是不是解释得通。

5. **优化层**  
   利用 `P_conn` 对填充率/数量大体单调的性质，做：
   - 问题 3：二分搜索最小填充率
   - 问题 4：二维边界搜索 + 成本最小化

这条主线现在已经不是停留在设想，而是已经通过 Q1-Q4 的代码和结果跑通了。

## 2. 各问题目前推进到哪里

### 问题 1：给定结构是否导通

这一问已经完全做成了一个稳定基线。

- 已经能读取附件中的三组数据
- 已经完成几何判距、建图、DSU 连通判定、贯通路径提取
- 已经把“周期连续”和“仅几何回绕”两种边界模式都做了
- 单元测试已通过

这一步的意义不是结果本身，而是把后面所有问题都依赖的**几何判据和连通性框架**打牢了。

### 问题 2：给定填充率时的导通概率

这一问已经把“概率云理论”和“Monte Carlo 仿真”真正接上了。

当前关键结果：

- `V_AA = 2.5493×10^9 nm^3`
- 在 `φ = 0.50%, 0.60%, 0.70%, 1.00%` 下，`P_conn` 随填充率上升明显增大
- 对应的平均连接度 `k̄` 分别约为 `0.902, 1.081, 1.262, 1.802`
- `k̄≈1` 附近正好是 `P_conn` 快速上升区

这说明：  
**概率云理论给出的平均连接度指标，确实能解释宏观导通概率的变化趋势。**

也就是说，Q2 已经把“局部连接概率 -> 宏观导通行为”这件事建立起来了。

### 问题 3：满足 `P_conn ≥ 90%` 的最低 A 填充率

这一问已经得到实测结果，而且已经和理论临界做了定量对照。

当前结果：

- `periodic_connected`：`φ_min = 0.5125%`
- `wrapped_geometry_only`：`φ_min = 0.4969%`
- 理论临界：`φ_theory = V_A / V_AA ≈ 0.5546%`

现在比较稳的解释是：

1. 理论上 `k̄=1` 给出的是一个**均值场意义下的临界参考值**
2. 实际有限尺寸系统里，细长圆柱的链式连接能力很强
3. 所以要达到 `P_conn ≥ 0.90`，实测最小填充率会**略低于** `φ_theory`

这一步非常关键，因为它已经把理论从“定性解释”推进到了“可定量对照”的层面。

另外，Q3 还有一条很强的证据：

- 另一台设备独立重跑后，`problem3_result.json/csv/confirm.csv` **逐字节一致**

这说明当前随机种子派生和评估顺序设计是可靠的，结果不是偶然跑出来的。

### 问题 4：A/B 混合体系最低成本组合

这一问也已经完整跑通，并且得到比较清楚的工程结论。

当前结果：

- `periodic_connected` 最优：`(N_A, N_B) = (360, 0)`，成本 `5.3438 元`
- `wrapped_geometry_only` 最优：`(N_A, N_B) = (340, 50)`，成本 `5.1307 元`

当前最稳的解释是：

1. **体系想真正稳定导通，主骨架仍然要靠 A 圆柱来承担**
2. B 球虽然单位体积便宜，但单个颗粒体积大、点接触导电效率不高
3. 所以纯 B 路线并不经济，往往需要很多很多颗球才够
4. B 的合理角色不是“替代 A 成为主体”，而是**在 A 已接近临界时做低成本桥接补强**

所以 Q4 的结论不是“B 更优”，而是：

**最优方案仍以 A 为主，B 只在临界区作为少量辅助。**

## 3. 现在已经形成的核心结论

如果把目前全部工作压成几句最关键的话，可以概括为：

1. **问题 1 给出了可靠的几何导通判定与连通性计算基线。**
2. **问题 2 说明概率云理论不是空想，它能解释导通概率随填充率上升的机理。**
3. **问题 3 说明概率云理论还能给出临界填充率的定量参考，且与仿真结果量级一致、方向一致。**
4. **问题 4 说明在满足导通概率约束下，最低成本方案不是靠大量 B 球堆出来，而是以 A 为主、B 为辅。**

把这四点连起来，现在整个项目最自然的叙述已经很清楚：

> 我们先从几何导通定义出发，建立随机填充导电介质的三维连通模型；  
> 再通过概率云把局部两体连接行为压缩成等效连接体积；  
> 进而用平均连接度和连接矩阵解释系统的渗流临界；  
> 最后用 Monte Carlo 在有限尺寸盒体中验证理论，并完成最低填充率和最低成本组合的优化。

## 4. 现在对“概率云理论是否正确”的判断

这个问题现在可以分成两个层次来讲。

### 4.1 如果说“验证是否成立”

目前答案可以说是：**成立，而且证据链已经比较完整。**

已有证据链：

1. **Q2：趋势验证**  
   `k̄≈1` 的区域和 `P_conn` 快速上升区对上了。

2. **Q3：定量验证**  
   `φ_min` 落在 `0.50%` 左右，而理论临界 `φ_theory≈0.5546%`，量级一致、方向一致。

3. **Q3 跨设备复现：独立复现验证**  
   另一台设备重跑结果逐字节一致，排除了“偶然种子碰巧”的担忧。

4. **Q4：理论继续可用**  
   概率云扩展到双介质后，`λ_max` 仍然能给出有意义的理论可行区，并与仿真边界保持一致趋势。

### 4.2 如果说“数学证明是否完成”

这点要分得更严谨一些：

- **还不能说做出了严格数学证明**
- 更准确的表达应当是：  
  **“概率云理论已得到多层次数值验证，并且和仿真结果高度一致。”**

这个表述更稳，也更适合论文和答辩。

## 5. 当前最适合写进论文的主叙述

如果现在开始组织论文正文，最适合的主叙述应该是下面这套：

### 主线一：从局部连接到宏观导通

- 局部层面：两导体是否导通由几何距离决定
- 统计层面：大量随机取向/随机位置后，局部连接可用概率云描述
- 宏观层面：局部连接概率云经过积分，形成等效连接体积，进而决定平均连接度或连接矩阵
- 最终：系统在临界区附近发生从不贯通到高概率贯通的转变

### 主线二：理论与仿真互相支撑

- 理论不是直接代替仿真
- 理论负责给出解释、临界参考、搜索方向
- 仿真负责给出有限尺寸下的真实 `P_conn` 和最终优化结果

所以论文里最好不要写成“我们提出一个纯理论公式直接算出全部答案”，而是写成：

> “我们建立了概率云—渗流解释框架，并用 Monte Carlo 对其进行验证和修正。”

### 主线三：工程优化结论

- 如果只看“能不能导通”，A 和 B 都能参与
- 但如果把“成本”也纳入目标，结论会发生变化
- 最优策略不是多放便宜 B，而是先用 A 建立高效主骨架，再用少量 B 在临界区做补强

这会让第四问的结论和前面的理论线形成自然闭环。

## 6. 当前还存在的边界与风险

虽然主线已经成形，但仍有几个地方在表述上要保持克制。

### 6.1 理论是“验证充分”，不是“严格证明”

这一点前面已经说过，后续写作时必须保持措辞准确。

### 6.2 Q3/Q4 个别点处于统计临界

例如：

- Q3 的 wrapped 模式下，最终 4000 次复核 `p_lower=0.8997`
- Q4 某些边界点在 500 次 trials 下满足，但 4000 次复核略低于 0.90

这不说明模型错了，而说明这些点本来就在临界区附近，统计波动会比较敏感。  
论文里应该如实记录，而不要把这些点说得过满。

### 6.3 目前仍然是有限尺寸盒体结论

现在所有结论都对应：

- 边长 `10000nm` 的盒体
- 当前给定的 A/B 几何参数
- 当前导通阈值 `δ=1.8nm`

因此当前结论严格说是**这组参数下的有限尺寸工程结论**，不是对所有尺度和所有参数都普适。

## 7. 现在最值得保留的几个数字

如果后面要做摘要、答辩或快速回顾，最值得记住的是这些数：

- `V_AA = 2.5493×10^9 nm^3`
- `φ_theory ≈ 0.5546%`
- `Q3 periodic φ_min = 0.5125%`
- `Q3 wrapped φ_min = 0.4969%`
- `Q4 periodic 最优 = (360, 0), 5.3438 元`
- `Q4 wrapped 最优 = (340, 50), 5.1307 元`

这些数字基本就是当前整套思路最重要的“锚点”。

## 8. 当前项目状态一句话总结

**现在项目已经不再是“只有仿真代码”，而是已经形成了“几何判据 + 概率云理论 + 渗流解释 + Monte Carlo 验证 + 优化求解”的完整方法链。**

如果继续往前推进，最自然的下一步不是再盲目补代码，而是：

1. 把这条方法链整理成论文叙述；
2. 把“理论解释”和“仿真结果”对应着写；
3. 对临界点的统计波动保持如实表述；
4. 把 Q4 的工程结论提炼成“以 A 为主、B 为辅的成本优化策略”。
